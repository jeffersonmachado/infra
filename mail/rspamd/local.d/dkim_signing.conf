# DKIM signing configuration for Rspamd
# Keys are stored in /var/lib/rspamd/dkim/

path = "/var/lib/rspamd/dkim/$DOMAIN-$SELECTOR.key";
selector = "mail";

domain {
    results.com.br {
        path = "/var/lib/rspamd/dkim/mail.results.com.br.key";
        selector = "mail";
    }
}
